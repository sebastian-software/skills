# skill-sync

TypeScript CLI for importing, validating, building, and installing shared agent
skills for Codex and Agents.

```bash
skill-sync validate
skill-sync build
skill-sync sync --target all
```
